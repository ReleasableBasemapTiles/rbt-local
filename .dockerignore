# Used by Dockerfile.mapproxy (context .). tileserver/fonts and
# tileserver/styles are bind-mounted at Compose runtime, not copied into
# any image; tileserver/data MBTiles are large enough to stall the upload.
.git
.cursor
**/.DS_Store

tileserver/data
tileserver/config
tileserver/fonts
tileserver/styles

mapproxy/config
mapproxy/data
mapproxy/locks
mapproxy/tile_locks

nginx
charts
docs
*.md

deploy.sh
deploy.ps1
docker-compose*.yaml
.env
.env.example
